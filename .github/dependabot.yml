version: 2
updates:
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    # PRs landen auf dev, nicht auf main.
    # Releases gehen ausschließlich per PR dev → main (Branch Protection).
    target-branch: "dev"
